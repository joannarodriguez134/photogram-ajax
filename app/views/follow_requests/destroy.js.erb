
console.log("unfollowed!!!")


var follow = $("<%= j(render "follow_requests/form", follow_request: @follow_request.recipient.received_follow_requests.build) %>")

$("#<%= dom_id(@follow_request) %>").replaceWith(follow)
